# Repository Rules

- When you bump a release version or run `make dist` / `make release` with `VERSION=...`, keep that version exactly the same as the git tag being created or pushed. The version and tag must match.
